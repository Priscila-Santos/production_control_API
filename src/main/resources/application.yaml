spring:
  application:
    name: production-control

  datasource:
    url: jdbc:postgresql://localhost:5434/production_db
    username: ${DB_USER}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
